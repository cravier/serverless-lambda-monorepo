{
  "name": "serverless-lambda-monorepo",
  "description": "Serverless lambda services built as a monorepo",
  "authors": "Qate - Clément Ravier",
  "version": "0.1.0",
  "main": "pathToMain",
  "scripts": {
    "preinstall": "npx -y only-allow pnpm",
    "services:info": "turbo run info --parallel",
    "services:deploy": "turbo run deploy --parallel"
  },
  "devDependencies": {
    "serverless-esbuild": "^1.45.0",
    "serverless-offline": "^12.0.4",
    "turbo": "^1.10.0",
    "only-allow": "^1.1.1"
  },
  "engines": {
    "node": ">=16",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@8.6.0"
}